{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview",
  "description": "Read-only note preview windows",
  "windows": ["floating-preview", "pdf-preview"],
  "permissions": ["core:event:default", "shell:allow-open"]
}
