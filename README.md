# Notch

A programmer's notebook app, inspired by Quiver.

## Development

Install dependencies:

```bash
bun install
```

Run in development mode:

```bash
bun run dev
```

Build for production:

```bash
bun run build
```

The built `.dmg` will be in `src-tauri/target/release/bundle/dmg/`.
